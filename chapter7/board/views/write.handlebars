<h1>[{{title}}] 글 {{#if (eq mode "create")}}작성{{else}}수정{{/if}}</h1>

<div>
    <form name="boardForm" method="post" action="/write">
    <div>
        <label>제목</label>
        <input type="text" name="title" value="{{post.title}}" placeholder="제목을 넣어주세요"/>
    </div>

    <div>
        <label>이름</label>
        <input type="text" name="writer" value="{{post.writer}}" placeholder="이름을 넣어주세요"/>
    </div>
    
    <div>
        <label>비밀번호</label>
        <input type="password" name="password" placeholder="비밀번호를 넣어주세요"/>
    </div>

    <div>
        <label>본문을 입력하세요</label><br>
        <textarea name="content" cols="50" rows="10" placeholder="본문">{{post.content}}</textarea>
        <br>
    </div>

    <div>
        <button type="submit">저장</button>
        <button type="button" onclick="location.href='/'">취소</button>
    </div>
    </form>
</div>